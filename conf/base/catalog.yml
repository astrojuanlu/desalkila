_callejeros_merge_save_args:
  mode: overwrite
  # merge_predicate: "s.COD_NDP = t.COD_NDP"

_registro_save_args:
  mode: overwrite

_vut_madrid_save_args:
  mode: overwrite

callejero_vigente_raw:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://callejero/bronze/callejero_vigente_raw
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args: ${_callejeros_merge_save_args}

callejero_historico_raw:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://callejero/bronze/callejero_historico_raw
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args: ${_callejeros_merge_save_args}

callejero:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://callejero/silver/callejero
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args: ${_callejeros_merge_save_args}

barrios:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://barrios/barrios
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args:
    mode: overwrite

registro_cam_raw:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://registro-cam/bronze/registro_cam_raw
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args: ${_registro_save_args}

registro_cam:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://registro-cam/silver/registro_cam
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args: ${_registro_save_args}

vut_madrid_raw:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://vut-madrid/bronze/vut_madrid_raw
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args: ${_vut_madrid_save_args}

matchings_registro:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://matchings/silver/matchings_registro
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args:
    mode: overwrite

airbnb_madrid_raw:
  type: desalkila.datasets.PolarsDeltaDataset
  filepath: s3://airbnb/bronze/airbnb_madrid_raw
  credentials: delta_lake
  storage_options: ${globals:delta_storage_options}
  save_args:
    mode: overwrite
